# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.writing/
dist

dist-ssr
*.tsbuildinfo
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
release
# electron/ sources are committed (main, preload, MCP). Do not ignore the whole dir.

# Backend server data
server/data/*.db
server/data/.encryption-key
server/dist/

 # Orchestrator internal files
.sisyphus/

# Claude Code local (per-developer) settings
.claude/settings.local.json

# Landing page (separate static site)
LandingPage/

# Release version synchronization runtime files
.release-version.lock
.release-version.lock.takenover
.package-sync.transaction.json
.package-sync-backups/
versions/.version-info.xml.*.tmp
package-lock.json.*.tmp
.omo/
.zcode/

# Local static demos for entry-point selection (never commit)
.tmp-chat-entry-demos/
